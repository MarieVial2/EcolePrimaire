{% extends 'base.html.twig' %}

{% block title %}Classes{% endblock %}

{% block body %}
    <h1>{{ class.nomClasse }}</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Eleves</th>
                
            </tr>
            <tr>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Date de naissance</th>
                
            </tr>
                {% for eleve in eleves %}
                <tr>
                    <td> {{ eleve.nomEleve }}</td>
                    <td> {{ eleve.prenomEleve }}</td> 
                     <td>{{ eleve.dateNaissance | date('d/m/Y')}}</td> 
                </tr>

                {% endfor %}
            
        </tbody>
    </table>

    <a href="{{ path('app_classes_index') }}">Retour à la liste</a>

    <a href="{{ path('app_classes_edit', {'id': class.id}) }}">Editer</a>

    {{ include('classes/_delete_form.html.twig') }}
{% endblock %}
